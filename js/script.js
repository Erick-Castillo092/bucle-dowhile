var intento;
var numerosecreto = 42;

do {
    intento = parseInt(prompt("Adivina el numero "));
    if (intento < numerosecreto)(
        alert ("El numero es mas grande");
     ) 
     if (intento > numerosecreto)(
        alert ("Es un numero mas pequeño");
    )
}while (intento !== numerosecreto);

  